---
description: Scaffold a new React component in this repo's style
argument-hint: <name> [what it should do]
---

Create a new component in this repo: **$ARGUMENTS**

Follow the `react-component` skill.

Before you start, state what you're about to create (files).
After the files exist, prove the build is intact:

```bash
npm run build
```

Report: the files created and the build result. If the build fails, fix it.
